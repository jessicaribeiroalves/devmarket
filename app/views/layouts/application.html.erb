<!DOCTYPE html>
<html>
  <head>
    <title>DevMarket</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%# NAVBAR. Split into partial later %>
    <ul>
      <li><%= link_to 'Register', new_user_registration_path, class:"" unless user_signed_in? %></i>
      <li><%= link_to 'Log In', new_user_session_path, class:"" unless user_signed_in? %></i>

      <li><%= link_to 'Edit Profile', edit_user_registration_path, class:"" if current_user %></i>
      <li><%= link_to 'Log Out', destroy_user_session_path, method: :delete, class:"" if current_user %></i>
    </ul>

    <%= yield %>
  </body>
</html>
